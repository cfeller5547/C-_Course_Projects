#pragma once
#include "PlayerClass.h"


class EventClass{

public:
	void thunderstorm(PlayerClass*);
	void bearattack(PlayerClass*);
	void biketirepop(PlayerClass*);
	void berries(PlayerClass*);

private:

};

